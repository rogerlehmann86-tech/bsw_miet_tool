const uk='gvos_demo_users_v03',sk='gvos_customer_session_v03';if(!localStorage.getItem(uk))localStorage.setItem(uk,JSON.stringify([{id:'user-default',username:'gemeinde',password:'demo2026',label:'Verbandsgemeinde',active:true}]));if(sessionStorage.getItem(sk))location.replace('index.html');document.getElementById('loginForm').onsubmit=e=>{e.preventDefault();const u=document.getElementById('loginUser').value.trim(),p=document.getElementById('loginPassword').value,user=JSON.parse(localStorage.getItem(uk)||'[]').find(x=>x.active!==false&&x.username===u&&x.password===p);if(!user){const n=document.getElementById('loginError');n.textContent='Benutzername oder Passwort ist nicht korrekt.';n.classList.remove('hidden');return}sessionStorage.setItem(sk,JSON.stringify({id:user.id,username:user.username,label:user.label}));location.replace('index.html')};
